<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
		<title>Problem - CS Club Online Judge</title>	
	</head>
	
	<body>			
			<main>
				<div id="content">
					<div class="innertube">
						<h1><%= @problem.title %></h1>
						Runtime: <%= @problem.runtime %>s
						<br/>
						Memory limit: <%= @problem.memory %>M
						<br/>
						<h2>Problem:</h2>
						<%= simple_format(@problem.problem) %>
						<%= link_to "Submit", "/problem/#{params[:contest_id]}/#{params[:problem_id]}/submit" %>

					</div>
				</div>
			</main>	
	</body>
</html>
